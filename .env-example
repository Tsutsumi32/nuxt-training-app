# コンテナ名
CONTAINER_NAME=nuxt-portfolio

# ポート番号（ホスト側:コンテナ側）
HOST_PORT=3000
CONTAINER_PORT=3000

# Node.js環境変数
NODE_ENV=development

# ユーザーIDとグループID（ホスト側のユーザーと一致させる）
# 確認方法: id -u と id -g で確認
UID=1000
GID=1000

# Nuxt設定（必要に応じて追加）
# NUXT_HOST=0.0.0.0
# NUXT_PORT=3000
